{
  "name": "ofh-portfolio",
  "version": "1.0.0",
  "description": "personal portfolio for objectsforheads",
  "dependencies": {},
  "scripts": {
    "compile": "bin/compile.js",
    "watch": "onchange 'content/**/*' 'layouts/**/*' 'lib/**/*' 'Gemfile' 'nanoc.yaml' 'Rules' -- npm run compile",
    "livereload": "browser-sync start --server 'output' --files './output/**/*'",
    "dev": "run-p watch livereload"
  },
  "author": "objectsforheads",
  "devDependencies": {
    "browser-sync": "^2.18.13",
    "npm-run-all": "^4.1.1",
    "onchange": "^3.2.1"
  }
}
